<%= simple_form_for(@download) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :design %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
